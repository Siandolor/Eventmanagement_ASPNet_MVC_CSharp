@model Eventmanagement.Models.Users.InternalCoworker

<h4>@(Model.InternalCoworker_FirstName)'s-Profil</h4>

<div class="row align-items-center">
    <div class="col-3">
        @{
            var rawPath = Model.InternalCoworker_ProfileImage;

            if (Model.InternalCoworker_IsBlocked)
            {
                rawPath = "~/images/default_coworker_blocked-V1.png";
            }
            else if (string.IsNullOrEmpty(rawPath))
            {
                rawPath = "~/images/default_coworker_active-V1.png";
            }
            else
            {
                rawPath += "?v=" + DateTime.Now.Ticks;
            }

            var imagePath = Url.Content(rawPath);
        }
        <img src="@imagePath" class="img-thumbnail mt-2 mb-2" style="width: 300px;" />
    </div>

    <div class="col-9">
        <div class="col-12 row mt-1 mb-2">
            <div class="col-4 fw-bold">Name</div>
            <div class="col-8">@Model.InternalCoworker_FirstName @Model.InternalCoworker_LastName</div>
        </div>

        <div class="col-12 row mt-1 mb-2">
            <div class="col-4 fw-bold">Rolle</div>
            <div class="col-8">@Model.InternalCoworker_Role</div>
        </div>

        <div class="col-12 row mt-1 mb-2">
            <div class="col-4 fw-bold">Geburtsdatum</div>
            <div class="col-8">@Model.InternalCoworker_DateOfBirth.ToShortDateString()</div>
        </div>

        <div class="col-12 row mt-1 mb-2">
            <div class="col-4 fw-bold">Registriert am</div>
            <div class="col-8">@Model.InternalCoworker_RegistrationDate</div>
        </div>

        <div class="col-12 row mt-1 mb-2">
            <div class="col-4 fw-bold">Benutzerstatus</div>
            <div class="col-8">@Model.InternalCoworker_IsBlocked</div>
        </div>
    </div>
</div>

<div class="row align-items-center mt-2">
    <div class="col-12 row d-row mt-1 mb-2">
        <div class="col-2 fw-bold">Biografie</div>
        <div class="col-10">
            @Model.InternalCoworker_ProfileBio
        </div>
    </div>
</div>
